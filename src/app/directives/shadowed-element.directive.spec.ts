import { ShadowedElementDirective } from './shadowed-element.directive';

describe('ShadowedElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ShadowedElementDirective();
    expect(directive).toBeTruthy();
  });
});
